name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./lib
    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: cd yarn
      - name: Run tests
        run: yarn test
